{
  "name": "@foresteam/field-selector",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run test && tsc",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "prepare": "npm run build"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "zod": "^3.25.67"
  }
}
